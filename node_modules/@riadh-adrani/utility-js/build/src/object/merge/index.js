"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const hasProperty_1 = __importDefault(require("../hasProperty"));
/**
 * deeply merge two or more objects.
 * @param objects to merge.
 */
function merge(...objects) {
    if (objects.some((o) => typeof o !== "object")) {
        throw new Error("Some/All arguments are not of type (object).");
    }
    return objects.reduce((output, object) => {
        Object.keys(object).forEach((key) => {
            if (!(0, hasProperty_1.default)(output, key)) {
                output[key] = object[key];
            }
            else {
                if (typeof object[key] !== "object" ||
                    typeof output[key] !== "object" ||
                    output[key] === null ||
                    object[key] === null) {
                    output[key] = object[key];
                }
                else {
                    output[key] = merge(output[key], object[key]);
                }
            }
        });
        return output;
    }, {});
}
exports.default = merge;
