To initialize: `npm i`

To build, do the following, run the following from the project root `npm run build`

To preview your changes locally run `npm run dev`


git add .
git commit -m "Description of changes"
git push origin main
